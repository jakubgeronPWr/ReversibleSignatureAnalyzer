using System;
using System.IO;
using NUnit.Framework;

namespace ReversibleSignatureAnalyzer.Test
{
    public class DwtTests
    {
        [SetUp]
        public void Setup()
        {
        }

        [Test]
        public void IsImageAfterInverseIsEqualToOrigin()
        {
        //var projectDirectory = Directory.GetParent(Environment.CurrentDirectory).Parent.Parent.FullName;
        //var importedImage = new BitmapImage(new Uri(projectDirectory + "/Model/_img/lena.png"));

        Assert.Pass();
        }

        
    }
}